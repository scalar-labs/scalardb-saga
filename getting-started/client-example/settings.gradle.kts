// A standalone build, deliberately not part of the repository's root build: it consumes the
// published client SDK from Maven Central exactly the way an application would.
rootProject.name = "scalardb-saga-client-example"
